#include<stdio.h>
#include<conio.h>
int cicle(int a, int b)
{
return a+b;
}

int main()
{
int a, b,sum;
printf("enter first and second number:");
scanf("%d %d",&a,&a);
sum = circle();
printf("the sum is:%d",sum)
getch();
return 0;
}
